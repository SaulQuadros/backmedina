streamlit>=1.49
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
plotly>=5.20
openpyxl>=3.1
python-docx>=1.1
# Extração de PDF FWD usa `pdftotext` (poppler-utils, dependência de SISTEMA —
# ver packages.txt / Dockerfile), não um pacote pip.
#
# pytest é só para desenvolvimento/testes — NÃO é dependência de runtime e por
# isso fica fora deste arquivo (que a nuvem instala). Para rodar os testes local:
#   pip install pytest>=7.0
